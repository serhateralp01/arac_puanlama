{
  "name": "arac-puanlama",
  "version": "2.0.0",
  "private": true,
  "description": "Türkiye ikinci el otomatik vitesli araç puanlama aracı — veri ve görünüm ayrılmış sürüm",
  "scripts": {
    "build": "python3 scripts/build.py",
    "check": "python3 scripts/validate.py && python3 scripts/build.py --check",
    "test": "node scripts/smoke_test.js"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
